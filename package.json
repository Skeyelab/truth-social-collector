{
  "name": "truth-social-collector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-extension.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "esbuild": "^0.25.10",
    "vitest": "^4.1.3"
  }
}
